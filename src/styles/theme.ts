// src/styles/theme.ts
import colors from './colors';
import typography from './typography';

const theme = {
  colors,
  typography,
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
  },
  borderRadius: 8,
};

export default theme;
// src/styles/theme.ts